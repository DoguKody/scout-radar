# Core Airflow (Composer 2.x usually supports Airflow 2.7+)
apache-airflow==2.7.3

# Google providers (BigQuery, GCS, etc.)
apache-airflow-providers-google==10.14.0

# Data & API
pandas==2.1.4
requests==2.31.0
python-dotenv==1.0.1

# Forecasting
prophet==1.1.5
